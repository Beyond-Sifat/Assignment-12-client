import React from 'react';
import { useParams } from 'react-router';

const PaymentForm = () => {
     const  id  = useParams();
     console.log(id)
    return (
        <div>
            hfhdhd
        </div>
    );
};

export default PaymentForm;